homepage:
  path:     /
  defaults: { _controller: MainBundle:Default:index }
dashboard:
  path:     /Dash
  defaults: { _controller: MainBundle:Default:dash}
dash:
  path:     /DashB
  defaults: { _controller: MainBundle:Default:dashB}
recaptcha:
  path:     /captcha
  defaults: { _controller: MainBundle:Default:recaptcha}
captchaverify:
  path:     /captchaverif
  defaults: { _controller: MainBundle:Default:captchaverify}
back:
  path:     /back
  defaults: { _controller: MainBundle:Default:back }
chartback:
  path:     /chartback
  defaults: { _controller: MainBundle:Default:chartback }
